#include <stdio.h>

int main(int argc, char const *argv[])
{
      double prices[] = {5.0, 10.0, 15.0, 25.0, 20.0, 30.0}; // declaration with assignment

     // double prices[5]; // declaring an array

     // prices[0] = 5.0;
     // prices[1] = 10.0;
     // prices[2] = 15.0;
     // prices[3] = 25.0;
     // prices[4] = 20.0;

     // for (int i = 0; i < sizeof(prices)/sizeof(prices[0]); i++) // iterating the loop by dividing the total size of an array by an element
     // {
     //      printf("$%.2lf\n", prices[i]);
     // }
     
     

     return 0;
}
