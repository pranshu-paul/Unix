#include <stdio.h>
#include <stdbool.h>

int main(int argc, char const *argv[])
{
     // logical operator = && (AND) check if two conditions are true
     // logical operator = || (OR) check if any condition is true
     // logical operator = ! (NOT) check if two conditions are true

     float temp = 25;
     bool sunny = true;

     if (temp <= 0 || temp >= 30)
     {
         printf("The weather is bad!\n");
     }
     else
         printf("The weather is good!\n");
     {
     }
     


     return 0;
}
